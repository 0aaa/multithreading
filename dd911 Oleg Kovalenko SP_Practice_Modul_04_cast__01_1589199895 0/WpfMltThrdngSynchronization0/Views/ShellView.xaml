<Window x:Class="WpfMltThrdngSynchronization0.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <ListBox ItemsSource="{Binding ThreadsSynchronizationResultArr}"/>
        <StackPanel Grid.Column="1">
            <Button Command="{Binding Exercises1and6Cmd}" Content="Exercises 1 and 6"/>
            <Button Command="{Binding Exercises2and3and7Cmd}" Content="Exercises 2, 3 and 7"/>
            <Button Command="{Binding Exercise5Cmd}" Content="Exercise 5"/>
        </StackPanel>
    </Grid>

</Window>